-rwxr-xr-x 1 root root 220 May 15 2012 var/lib/dpkg/info/adduser.postrm

#! /bin/sh
set -e

if [ "$1" = "purge" ]; then
    rm -f /etc/adduser.conf /etc/adduser.conf.dpkg-save
    if [ -e /usr/share/debconf/confmodule ]; then
        . /usr/share/debconf/confmodule
        db_purge
    fi
fi
