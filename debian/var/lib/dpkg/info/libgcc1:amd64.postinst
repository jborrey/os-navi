-rwxr-xr-x 1 root root 300 Dec 25 2014 var/lib/dpkg/info/libgcc1:amd64.postinst

#! /bin/sh -e

case "$1" in
    configure)
	docdir=/usr/share/doc/libgcc1
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	    rm -rf $docdir
	    ln -s gcc-4.9-base $docdir
	fi
esac

# Automatically added by dh_makeshlibs
if [ "$1" = "configure" ]; then
	ldconfig
fi
# End automatically added section

